{
  "name": "textile-erp",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Root package for Vercel deployment - provides dependencies for the /api serverless function that wraps the Express backend.",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev:backend": "cd backend && npm install && npm run dev",
    "dev:frontend": "cd frontend && npm install && npm run dev"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.19.2",
    "mysql2": "^3.11.3"
  }
}
